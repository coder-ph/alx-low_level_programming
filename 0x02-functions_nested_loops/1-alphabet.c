#include "main.h"
/**
 * print_alphabet - the function prints out the alphabets
 * main - code checking
 * Return: o, success
*/
void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);
	_putchar('\n');
}
